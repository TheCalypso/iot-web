import { createAction } from '@ngrx/store';

export const toggle = createAction('[Sidebar visivility] toggle');
export const close = createAction('[Sidebar visivility] close');
export const open = createAction('[Sidebar visivility] open');
